# Presentation System Memory

## Builder And Publishing
- Configure branding, eligible models, enhancements, privacy, contact, preview, and publishing.
- Generate explicit package family/schema/runtime/capability metadata.
- Deliver owner-hostable packages; publishing does not auto-activate Atlas or send outreach.

## Runtime And HUD
- Preserve renderer navigation whenever overlay tools are inactive.
- Keep direct-host and Atlas-modal behavior aligned.
- Fail safely when optional dependencies are unavailable.
- Runtime changes require new-package generation plus a supported upgrade path where compatible.

## Explore Together
- Desktop-only host/guest PeerJS data and audio.
- Two-way Matterport `U` + Copy view sync.
- Sequential shared Draw, Focus Rope, Eraser, Clear, floor heartbeat, and reconnect safety.
- Voice failure must not destroy the data session.
- Phones/tablets expose no collaboration UI.

## Upgrade Center
- Inspect uploaded HTML as inert text.
- Patch only approved sentinel/meta regions using trusted bundled sources.
- Preserve untouched bytes; verify source/output SHA-256 and current-version reinspection.
- Never execute, retain, downgrade, or authorize a failed output.
- P1-P4 merged; P5 is PR #170; P6 self-service remains held until P5 acceptance.
